version: "3.8"

services:
  db:
    image: postgres:15-alpine
    container_name: pos_db
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: always

volumes:
  pgdata:
